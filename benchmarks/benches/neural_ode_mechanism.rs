{
  "path": "benchmarks/benches/neural_ode_mechanism.rs",
  "content": "//! Neural ODE + Mechanism Design Benchmarks — Sprint 112 (v11.2.0)\n//!\n//! Run with: `cargo bench -p ed2kIA-benchmarks --bench neural_ode_mechanism`\n//!\n//! Target metrics:\n//! - Neural ODE Euler step (dim=64): < 5ms\n//! - Neural ODE RK4 step (dim=64): < 20ms\n//! - Flowpipe computation (dim=32, steps=10): < 100ms\n//! - Certificate generation (dim=32): < 50ms\n//! - VCG Auction (50 contributors): < 10ms\n//! - Shapley MC (20 contributors, 100 samples): < 50ms\n//! - Collective Mechanism round (30 peers): < 100ms\n\nuse criterion::{black_box, criterion_group, criterion_main, BenchmarkId, Criterion};\nuse std::time::Instant;\n\n// ============================================================\n// Neural ODE Benchmarks\n// ============================================================\n\n/// Simulated Neural ODE field evaluation: f(x) = Wx + b + activation\nstruct OdeFieldSim {\n    weight: Vec<f32>,\n    bias: Vec<f32>,\n    dim: usize,\n}\n\nimpl OdeFieldSim {\n    fn new(dim: usize) -> Self {\n        let mut rng = StdRng::seed_from_u64(42);\n        Self {\n            weight: (0..dim * dim).map(|_| rng.gen_range(-1.0..1.0)).collect(),\n            bias: (0..dim).map(|_| rng.gen_range(-0.5..0.5)).collect(),\n            dim,\n        }\n    }\n\n    fn evaluate(&self, x: &[f32]) -> Vec<f32> {\n        let mut out = vec![0.0f32; self.dim];\n        for j in 0..self.dim {\n            let mut sum = self.bias[j];\n            for i in 0..self.dim {\n                sum += x[i] * self.weight[i * self.dim + j];\n            }\n            // Tanh activation (bounded)\n            out[j] = sum.tanh();\n        }\n        out\n    }\n}\n\n/// Simulated Euler step: x(t+dt) = x(t) + dt * f(x(t))\nfn euler_step(field: &OdeFieldSim, x: &mut [f32], dt: f32) {\n    let f_x = field.evaluate(x);\n    for i in 0..x.len() {\n        x[i] += dt * f_x[i];\n    }\n}\n\n/// Simulated RK4 step\nfn rk4_step(field: &OdeFieldSim, x: &mut [f32], dt: f32) {\n    let k1 = field.evaluate(x);\n    let x_tmp = x.iter().zip(k1.iter()).map(|(xi, ki)| xi + dt * ki * 0.5).collect::<Vec<_>>();\n    let k2 = field.evaluate(&x_tmp);\n    let x_tmp2 = x.iter().zip(k2.iter()).map(|(xi, ki)| xi + dt * ki * 0.5).collect::<Vec<_>>();\n    let k3 = field.evaluate(&x_tmp2);\n    let x_tmp3 = x.iter().zip(k3.iter()).map(|(xi, ki)| xi + dt * ki).collect::<Vec<_>>();\n    let k4 = field.evaluate(&x_tmp3);\n    for i in 0..x.len() {\n        x[i] += dt * (k1[i] + 2.0 * k2[i] + 2.0 * k3[i] + k4[i]) / 6.0;\n    }\n}\n\n/// Simulated flowpipe computation: sequence of Euler steps\nfn flowpipe_euler(field: &OdeFieldSim, x0: &[f32], dt: f32, steps: usize) -> Vec<Vec<f32>> {\n    let mut x = x0.to_vec();\n    let mut trajectory = vec![x.clone()];\n    for _ in 0..steps {\n        euler_step(field, &mut x, dt);\n        trajectory.push(x.clone());\n    }\n    trajectory\n}\n\n/// Simulated flowpipe with RK4\nfn flowpipe_rk4(field: &OdeFieldSim, x0: &[f32], dt: f32, steps: usize) -> Vec<Vec<f32>> {\n    let mut x = x0.to_vec();\n    let mut trajectory = vec![x.clone()];\n    for _ in 0..steps {\n        rk4_step(field, &mut x, dt);\n        trajectory.push(x.clone());\n    }\n    trajectory\n}\n\n// ============================================================\n// Mechanism Design Benchmarks\n// ============================================================\n\n/// Simulated contribution\nstruct ContribSim {\n    peer_id: usize,\n    vfe_reduction: f32,\n    cost: f32,\n    trust: f32,\n    verified: bool,\n}\n\n/// Simulated VCG auction\nfn vcg_auction(contributions: &[ContribSim], max_winners: usize) -> Vec<usize> {\n    let mut indexed: Vec<_> = contributions.iter().enumerate().collect();\n    indexed.sort_by(|a, b| {\n        b.1.vfe_reduction\n            .partial_cmp(&a.1.vfe_reduction)\n            .unwrap_or(std::cmp::Ordering::Equal)\n    });\n    indexed\n        .into_iter()\n        .take(max_winners)\n        .filter(|(_, c)| c.verified && c.vfe_reduction > c.cost)\n        .map(|(i, _)| i)\n        .collect()\n}\n\n/// Simulated Shapley value computation (Monte Carlo)\nfn shapley_mc(contributions: &[ContribSim], num_samples: usize) -> Vec<f32> {\n    let n = contributions.len();\n    let mut values = vec![0.0f32; n];\n    let mut rng = StdRng::seed_from_u64(123);\n\n    for _ in 0..num_samples {\n        // Random permutation\n        let mut perm: Vec<usize> = (0..n).collect();\n        perm.shuffle(&mut rng);\n\n        // Marginal contributions\n        let mut marginal = vec![0.0f32; n];\n        let mut coalition_value = 0.0f32;\n        for &i in &perm {\n            marginal[i] = contributions[i].vfe_reduction - coalition_value;\n            coalition_value += contributions[i].vfe_reduction;\n        }\n\n        for i in 0..n {\n            values[i] += marginal[i];\n        }\n    }\n\n    for v in &mut values {\n        *v /= num_samples as f32;\n    }\n    values\n}\n\n/// Simulated MAD-based outlier detection\nfn mad_detect(contributions: &[ContribSim], threshold: f32) -> Vec<usize> {\n    let n = contributions.len();\n    if n < 3 {\n        return vec![];\n    }\n\n    let values: Vec<f32> = contributions.iter().map(|c| c.vfe_reduction).collect();\n    let median = compute_median(&values);\n    let abs_devs: Vec<f32> = values.iter().map(|v| (v - median).abs()).collect();\n    let mad = compute_median(&abs_devs);\n    if mad < 1e-10 {\n        return vec![];\n    }\n\n    let mut outliers = Vec::new();\n    for (i, v) in values.iter().enumerate() {\n        let modified_z = 0.6745 * (v - median).abs() / mad;\n        if modified_z > threshold {\n            outliers.push(i);\n        }\n    }\n    outliers\n}\n\nfn compute_median(values: &[f32]) -> f32 {\n    let mut sorted = values.to_vec();\n    sorted.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));\n    let n = sorted.len();\n    if n % 2 == 0 {\n        (sorted[n / 2 - 1] + sorted[n / 2]) / 2.0\n    } else {\n        sorted[n / 2]\n    }\n}\n\n/// Simulated full Collective Mechanism round\nfn collective_round(\n    contributions: &[ContribSim],\n    max_winners: usize,\n    shapley_samples: usize,\n    byzantine_threshold: f32,\n) -> (Vec<usize>, Vec<f32>, Vec<usize>) {\n    // Step 1: Filter Byzantine\n    let outliers = mad_detect(contributions, byzantine_threshold);\n    let clean: Vec<_> = contributions\n        .iter()\n        .enumerate()\n        .filter(|(i, _)| !outliers.contains(i))\n        .map(|(_, c)| *c)\n        .collect();\n\n    // Step 2: VCG auction\n    let winners = vcg_auction(&clean, max_winners);\n\n    // Step 3: Shapley values\n    let winner_contribs: Vec<_> = winners.iter().map(|&i| clean[i]).collect();\n    let shapley_values = shapley_mc(&winner_contribs, shapley_samples);\n\n    (winners, shapley_values, outliers)\n}\n\n// ============================================================\n// Criterion Benchmarks\n// ============================================================\n\nfn benchmark_ode_euler(c: &mut Criterion) {\n    let mut group = c.benchmark_group(\"neural_ode_euler\");\n\n    for dim in [16, 32, 64, 128, 256] {\n        let field = OdeFieldSim::new(dim);\n        let mut x: Vec<f32> = (0..dim).map(|i| (i as f32) * 0.1).collect();\n        let dt = 0.01;\n\n        group.bench_with_input(BenchmarkId::from_parameter(dim), &dim, |b, _| {\n            b.iter(|| {\n                let mut x_copy = x.clone();\n                euler_step(black_box(&field), black_box(&mut x_copy), black_box(dt));\n                x_copy\n            })\n        });\n    }\n\n    group.finish();\n}\n\nfn benchmark_ode_rk4(c: &mut Criterion) {\n    let mut group = c.benchmark_group(\"neural_ode_rk4\");\n\n    for dim in [16, 32, 64, 128] {\n        let field = OdeFieldSim::new(dim);\n        let mut x: Vec<f32> = (0..dim).map(|i| (i as f32) * 0.1).collect();\n        let dt = 0.01;\n\n        group.bench_with_input(BenchmarkId::from_parameter(dim), &dim, |b, _| {\n            b.iter(|| {\n                let mut x_copy = x.clone();\n                rk4_step(black_box(&field), black_box(&mut x_copy), black_box(dt));\n                x_copy\n            })\n        });\n    }\n\n    group.finish();\n}\n\nfn benchmark_flowpipe(c: &mut Criterion) {\n    let mut group = c.benchmark_group(\"flowpipe\");\n\n    let configs: &[(usize, usize)] = &[\n        (32, 10),\n        (32, 50),\n        (64, 10),\n        (64, 50),\n    ];\n\n    for &(dim, steps) in configs {\n        let field = OdeFieldSim::new(dim);\n        let x0: Vec<f32> = (0..dim).map(|i| (i as f32) * 0.1).collect();\n        let dt = 0.01;\n\n        group.bench_with_input(\n            BenchmarkId::from_parameter(format!(\"dim={dim},steps={steps}\")),\n            &(dim, steps),\n            |b, _| {\n                b.iter(|| {\n                    flowpipe_euler(\n                        black_box(&field),\n                        black_box(&x0),\n                        black_box(dt),\n                        black_box(steps),\n                    )\n                })\n            },\n        );\n    }\n\n    group.finish();\n}\n\nfn benchmark_vcg_auction(c: &mut Criterion) {\n    let mut group = c.benchmark_group(\"vcg_auction\");\n\n    let mut rng = StdRng::seed_from_u64(42);\n    for n_contribs in [10, 50, 100, 500] {\n        let contributions: Vec<ContribSim> = (0..n_contribs)\n            .map(|i| ContribSim {\n                peer_id: i,\n                vfe_reduction: rng.gen_range(0.1..1.0),\n                cost: rng.gen_range(0.01..0.3),\n                trust: rng.gen_range(0.5..1.0),\n                verified: rng.gen_bool(0.9),\n            })\n            .collect();\n        let max_winners = (n_contribs / 10).max(1);\n\n        group.bench_with_input(BenchmarkId::from_parameter(n_contribs), &n_contribs, |b, _| {\n            b.iter(|| {\n                vcg_auction(\n                    black_box(&contributions),\n                    black_box(max_winners),\n                )\n            })\n        });\n    }\n\n    group.finish();\n}\n\nfn benchmark_shapley_mc(c: &mut Criterion) {\n    let mut group = c.benchmark_group(\"shapley_mc\");\n\n    let mut rng = StdRng::seed_from_u64(42);\n    let configs: &[(usize, usize)] = &[\n        (5, 100),\n        (10, 100),\n        (20, 100),\n        (20, 500),\n    ];\n\n    for &(n_contribs, samples) in configs {\n        let contributions: Vec<ContribSim> = (0..n_contribs)\n            .map(|i| ContribSim {\n                peer_id: i,\n                vfe_reduction: rng.gen_range(0.1..1.0),\n                cost: rng.gen_range(0.01..0.3),\n                trust: rng.gen_range(0.5..1.0),\n                verified: true,\n            })\n            .collect();\n\n        group.bench_with_input(\n            BenchmarkId::from_parameter(format!(\"n={n_contribs},samples={samples}\")),\n            &(n_contribs, samples),\n            |b, _| {\n                b.iter(|| {\n                    shapley_mc(black_box(&contributions), black_box(samples))\n                })\n            },\n        );\n    }\n\n    group.finish();\n}\n\nfn benchmark_byzantine_detection(c: &mut Criterion) {\n    let mut group = c.benchmark_group(\"byzantine_detection\");\n\n    let mut rng = StdRng::seed_from_u64(42);\n    for n_contribs in [20, 100, 500, 1000] {\n        let contributions: Vec<ContribSim> = (0..n_contribs)\n            .map(|i| ContribSim {\n                peer_id: i,\n                vfe_reduction: if i == 0 {\n                    10.0 // Outlier\n                } else {\n                    rng.gen_range(0.1..1.0)\n                },\n                cost: rng.gen_range(0.01..0.3),\n                trust: rng.gen_range(0.5..1.0),\n                verified: true,\n            })\n            .collect();\n\n        group.bench_with_input(BenchmarkId::from_parameter(n_contribs), &n_contribs, |b, _| {\n            b.iter(|| {\n                mad_detect(black_box(&contributions), black_box(3.0))\n            })\n        });\n    }\n\n    group.finish();\n}\n\nfn benchmark_collective_round(c: &mut Criterion) {\n    let mut group = c.benchmark_group(\"collective_round\");\n\n    let mut rng = StdRng::seed_from_u64(42);\n    for n_peers in [10, 30, 50] {\n        let contributions: Vec<ContribSim> = (0..n_peers)\n            .map(|i| ContribSim {\n                peer_id: i,\n                vfe_reduction: rng.gen_range(0.1..1.0),\n                cost: rng.gen_range(0.01..0.3),\n                trust: rng.gen_range(0.5..1.0),\n                verified: rng.gen_bool(0.9),\n            })\n            .collect();\n        let max_winners = (n_peers / 5).max(1);\n        let shapley_samples = 100;\n        let byzantine_threshold = 3.0;\n\n        group.bench_with_input(BenchmarkId::from_parameter(n_peers), &n_peers, |b, _| {\n            b.iter(|| {\n                collective_round(\n                    black_box(&contributions),\n                    black_box(max_winners),\n                    black_box(shapley_samples),\n                    black_box(byzantine_threshold),\n                )\n            })\n        });\n    }\n\n    group.finish();\n}\n\n// Import for RNG\nuse rand::rngs::StdRng;\nuse rand::{Rng, SeedableRng, seq::SliceRandom};\n\ncriterion_group!\n    name = neural_ode_mechanism_benches;\n    config = Criterion::default().sample_size(200);\n    targets =\n        benchmark_ode_euler,\n        benchmark_ode_rk4,\n        benchmark_flowpipe,\n        benchmark_vcg_auction,\n        benchmark_shapley_mc,\n        benchmark_byzantine_detection,\n        benchmark_collective_round\n);\ncriterion_main!(neural_ode_mechanism_benches);\n"}